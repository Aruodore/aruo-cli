# AI agent entry point

Read `.aruo/contract.yaml` and every applicable file in `.aruo/rules/` before changing this repository. Treat them as the engineering contract, not suggestions.

Keep `aruo.yaml` honest as production concerns change. Before declaring work complete, run the repository's relevant quality checks, inspect the diff, reconcile tests and documentation, and report anything not verified.

Never weaken validation, authorization, security controls, tests, or quality gates merely to make a change pass. Never commit secrets. Ask before destructive operations, dependency changes without a clear need, or architectural changes outside the task.
