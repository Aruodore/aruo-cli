# Internal implementation

Private Go packages described in [the architecture guide](../docs/architecture/README.md) live here. The initial `buildinfo` and `cli` packages form the executable shell. Future package boundaries follow domain responsibility, not technical layer dumping grounds. See the [CLI application architecture](../docs/architecture/cli-application.md).
