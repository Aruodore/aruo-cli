# Troubleshooting

Future troubleshooting pages use symptom → likely causes → safe diagnosis → recovery → prevention. They include debug-reference handling, configuration provenance, plugin trust/protocol failures, artifact verification, plan conflicts, interrupted transactions, cache repair, network/offline behavior, platform paths, and release verification. Commands must not ask users to delete broad directories or expose secrets.

