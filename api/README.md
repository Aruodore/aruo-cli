# API and schema contracts

This directory will hold versioned JSON Schemas for configuration, plans, findings, artifacts, evidence, and the plugin protocol. Generated bindings are outputs, not the source of truth. Compatibility and fixtures accompany every schema.

