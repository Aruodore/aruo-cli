# Development configuration

Shared tool configuration fragments and their rationale live here when root-standard filenames are not required. User/project configuration belongs in `aruo.yaml`; secrets never belong here.

