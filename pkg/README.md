# Public Go packages

Reserved for intentionally supported external Go APIs. It should remain empty until a concrete consumer, compatibility contract, and API review justify exposure. Internal code is not promoted for convenience.

